import random
test_list = [random.randint(1,100) for i in range(20)]
print(test_list)


def quick_sort(ls, start, end):
    flag = ls[start]
    index = start+1
    while index >
        if ls[index]>flag:
